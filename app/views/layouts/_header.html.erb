<header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">

      <%= link_to "Kilómetro Siete", '/', id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <% if user_signed_in? %>


              <li id= "fat-menu">
                <%=link_to "Welcome back, " + current_user.name , current_user %>
              </li>

              <li id= "fat-menu">
                  <%= link_to "Sign Out", destroy_user_session_path, method: "delete" %>
              </li>
          <% else %>
            <!--li id= "fat-menu">
              < %= link_to "Sign up", new_user_path %> 
            </li-->
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
