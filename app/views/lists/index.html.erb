<% provide(:title, 'Listas') %>
<h1>Mis Listas</h1>

<%= will_paginate %>

<%= link_to 'Lista nueva',  crear_lista_path %>

<ul class="users">
	<% @lists.each do |list| %>
		<table>
	        <tr>
	          <td><%= list.id %></td>
	          <td><%= list.name %></td>
	          <td><%= link_to ' Mostrar ', list %></td>
	          <td><%= link_to '| Editar', edit_list_path(list) %></td>
	          <td><%= link_to '| Destruir ', list, :confirm => '¿Está seguro?', 
	           :method => :delete %></td>
	        </tr>   
      	</table>	
    <% end %>

    <%= link_to "Reportes", problems_path %>
</ul>

<%= will_paginate %>