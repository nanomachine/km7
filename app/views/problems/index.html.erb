<% provide(:title, 'Problemas') %>
<h1>Problemas</h1>

<%= will_paginate %>

<ul class="users">
	<%= gmaps4rails(@json) %>
	<% @problems.each do |problem| %>
		<%= render @problems %>
	<% end %>
</ul>

<%= will_paginate %>